# Backlog

- Add a species-identifier wrangling wizard for structured tip labels, allowing users to preview and select the token, delimiter, regular expression, or NCBI taxon ID field used for taxonomy mapping.
